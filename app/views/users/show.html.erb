<h1 class="page-header"><%= @playlist.name %></h1>

<ul>
  <% @playlist.tracks.each do |track| %>
    <li><%= "#{track.name} - #{track.artists.map { |artist| artist.name }.join(", ")}" %></li>
  <% end %>
</ul>