<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% if @current_user.respond_to?(:uid) %>
        <h1>Here are your playlists: </h1>
                <div class="container">
                  <div class="row">
                    <div class="col-md-4">
                      <% @user_playlists.each do |playlist| %>

                          <h3 class="playlist-name"><%= playlist.name %></h3>
                          <h8><%= playlist.description %></h8>
                                <br></br>
                                <% playlist.tracks.each do |track| %>
                                <h5 class="trackdata"style="text-align:left;display: inline;"><%= track.name%></h5> <p style="display: inline;"><%= track.popularity %></p>
                              <br></br>
                              <% end %>
                          <hr>
                      <% end %>
                    </div>
                  </div>
                </div>
      <% else %>
        <h1>Login to view your playlists</h1>
      <% end %>
    </div>
  </div>
</div>


